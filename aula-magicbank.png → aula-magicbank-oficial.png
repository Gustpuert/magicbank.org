<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <title>Aula MagicBank | La educación del futuro, hoy</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600&display=swap" rel="stylesheet">

  <style>
    :root {
      --azul-profundo: #070C2A;
      --dorado: #D6B15A;
      --blanco: #FFFFFF;
    }

    * { margin: 0; padding: 0; box-sizing: border-box; }

    body {
      background: var(--azul-profundo);
      font-family: 'Inter', sans-serif;
      color: var(--blanco);
      text-align: center;
      overflow-x: hidden;
    }

    header {
      padding: 20px;
      border-bottom: 1px solid rgba(255,255,255,0.1);
    }

    .logo {
      font-size: 32px;
      font-weight: 600;
      color: var(--dorado);
      letter-spacing: 1px;
      animation: halo 4s ease-in-out infinite;
    }

    .subtitulo {
      font-size: 20px;
      color: #fff5d9;
      margin-top: 6px;
    }

    main {
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      padding: 40px 20px;
    }

    /* Imagen del aula */
    .imagen-aula {
      max-width: 1080px;
      width: 100%;
      border-radius: 12px;
      box-shadow: 0 0 18px rgba(214,177,90,0.3);
      animation: fadeIn 2s ease forwards;
    }

    /* Animación del logo palpitante */
    .logo-androide {
      position: absolute;
      top: 54%;
      left: 50%;
      width: 28px;
      height: 28px;
      border-radius: 50%;
      background: radial-gradient(circle, #FFD36A 0%, #D6B15A 60%, #704E1F 100%);
      transform: translate(-50%, -50%);
      animation: palpitar 4s ease-in-out infinite;
      box-shadow: 0 0 14px rgba(214,177,90,0.6);
    }

    /* Animaciones */
    @keyframes halo {
      0%, 100% { text-shadow: 0 0 10px rgba(214,177,90,0.6); }
      50% { text-shadow: 0 0 25px rgba(214,177,90,1); }
    }

    @keyframes fadeIn {
      from { opacity: 0; transform: translateY(30px); }
      to { opacity: 1; transform: translateY(0); }
    }

    @keyframes palpitar {
      0%, 100% { transform: translate(-50%, -50%) scale(1); opacity: 1; }
      50% { transform: translate(-50%, -50%) scale(1.3); opacity: 0.8; }
    }

    footer {
      font-size: 13px;
      color: rgba(255,255,255,0.6);
      padding: 30px;
      border-top: 1px solid rgba(255,255,255,0.1);
      margin-top: 60px;
    }

    @media (max-width: 768px) {
      .logo { font-size: 26px; }
      .subtitulo { font-size: 16px; }
    }
  </style>
</head>

<body>
  <header>
    <div class="logo">MagicBank</div>
    <div class="subtitulo">La educación del futuro, hoy</div>
  </header>

  <main id="aula-magicbank-oficial">
    <div class="aula-container">
      <img 
        src="assets/img/hero/aula-magicbank-oficial.png" 
        alt="Aula MagicBank - Ecosistema educativo inteligente"
        class="imagen-aula">

      <!-- Logo MB palpitante en el pecho del androide -->
      <div class="logo-androide"></div>
    </div>
  </main>

  <footer>
    MagicBank © Ecosistema educativo guiado por Inteligencia Artificial
  </footer>
</body>
</html>
